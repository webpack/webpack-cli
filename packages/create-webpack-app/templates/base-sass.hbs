{{#projectName}}
  /src /public /package.json /README.md /webpack.config.js /styles /styles.scss
{{/projectName}}
{{#if selectedSass}}
  // Add loader configuration for Sass in webpack.config.js module.exports = { // ... existing
  configuration from base.hbs (replace with actual configuration) module: { rules: [ // ... existing
  rules from base.hbs { test: /\.scss$/, use: [ 'style-loader', 'css-loader', 'sass-loader', ], },
  ], }, };
{{/if}}