//TODO: Get the inq prompt and return the parser once again until there's no packages left to validate.

module.exports = function resolveTransform(option) { //eslint-disable-line
	console.log('resolved');
};
