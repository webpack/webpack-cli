---
"webpack-cli": minor
---

Support `tsx` as a fallback loader for TypeScript configuration files (`.ts`, `.tsx`, `.cts` and `.mts`), used when none of the loaders known to `interpret` (such as `ts-node`) are installed.
